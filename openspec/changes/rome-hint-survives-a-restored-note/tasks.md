# rome-hint-survives-a-restored-note — tasks

- [ ] 1.1 Pin the reproducer from the proposal as a failing test.
- [ ] 1.2 Find why the plan skips the strike; decide whether the cause is in
      Rome or in the shared candidate plan.
- [ ] 1.3 Fix, prove the pinned test red without the fix, and run the
      cross-game hint walks for every game the fix reaches.
