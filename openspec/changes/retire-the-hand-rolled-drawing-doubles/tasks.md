# Tasks

- [ ] Re-take the population by shape and record the baseline counts.
- [ ] Move each hand-rolled `GameDrawing` double onto `RecordingDrawing` (or `renderScenario`).
- [ ] Verify by shape: per-file `it`/`expect(` counts and vitest total unchanged; a planted recorder defect goes red.
