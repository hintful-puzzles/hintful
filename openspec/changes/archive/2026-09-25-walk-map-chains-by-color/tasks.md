# walk-map-chains-by-color — tasks

- [x] 1 `chainSentence` reads the chain once, before the firing's narrowing
      legs: the pattern when every region has a dot of the struck color,
      otherwise the colored walk; throws if the walk misses the struck color.
- [x] 2 `say.chain` (walk, falling back to the rule past five regions) and
      `say.chainAlternates`.
- [x] 3 `FOUR_NAMES`: "violet".
- [x] 4 Tests: the three chain shapes pinned as descs; each named color a dot of
      its region and different from the one before (the first cut checked only
      the former and passed an off-by-one walk, so the second half was added and
      shown to fail on it); a listed walk ends on the struck color. The chain
      ledger entry re-keyed.
- [x] 5 `docs/games/hints.md` Map section.
