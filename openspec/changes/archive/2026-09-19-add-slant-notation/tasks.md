# add-slant-notation — tasks

## 1. Measure what the notation must express

- [x] 1.1 Record every equivalence merge's reason and parents. Take the distribution
      of chain lengths behind an equivalence step, and of the distance between the
      squares a link joins. Read what the mark must express off the chains, not the
      links (Loopy's lesson). `SlantTrace`; the figures are `design.md` D1, and
      they found a second arm the audit missed (clue firings counting a pair).

## 2. The notation

- [x] 2.1 Design the mark and its input across mouse, keyboard and touch; write
      `design.md`.
- [x] 2.2 Seed player notes into the solver; have `findMistakes` vouch for them.

## 3. The hint

- [x] 3.1 Place each cited link as a note step beside the firing that cites it.
- [x] 3.2 Rewrite `say.equiv` against the notes it now cites, and the clue
      firings that count a marked pair.
- [x] 3.3 Retire `docs/games/hints.md` § "The honest chain tier", repointing its
      citations. Its connectivity mechanics are § "Connectivity evidence (Slant)";
      what the notation taught is in § "Give the facts a notation (Loopy)".
- [x] 3.4 Update `help/games/slant.md` for the new mark.

## 4. Done

- [x] 4.1 Run the app.
- [x] 4.2 Owner acceptance: a new input and a new mark on the board (2026-09-19).
