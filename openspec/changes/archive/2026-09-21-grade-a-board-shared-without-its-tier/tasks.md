# grade-a-board-shared-without-its-tier — tasks

- [x] 1.1 Reproduce the owner's board: the hint refuses at move 10 when loaded
      by its short id, the board solves only at Tricky, and the commit before
      `bridges-hint-cites-an-unwritable-cap` refuses at the same move.
- [x] 1.2 Put the three shapes to the owner (grade on load, put the difficulty
      in the id, leave it). Chosen: grade on load.
- [x] 2.1 Grade an ambiguous `:desc` id in `Midend.newGameFromId`.
- [x] 2.2 Reload a shared board per tiered game in `difficulty-contract.test.ts`,
      with a sweep-wide count; prove it red with grading off.
- [x] 2.3 Pin the owner's board in `bridges-hint.test.ts`.
- [x] 2.4 Spec deltas: ADDED on `ts-engine`, MODIFIED on `app-shell`.
