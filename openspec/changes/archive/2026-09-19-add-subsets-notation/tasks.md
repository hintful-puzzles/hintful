# add-subsets-notation — tasks

## 1. Measure

- [x] 1.0 List every rule that reads `cube`, not only the two the audit named
      (proposal § "Carried from `add-slant-notation`"). `design.md` D1: only the
      advanced-arrow rule derives what the board cannot show.
- [x] 1.1 Per collapse and `singlePosition` (and any other arm 1.0 finds), count
      the cube eliminations it rests on that the board does not show, and how deep
      each one's chain runs. `design.md` D3.
- [x] 1.2 Census the eliminations' reasons by shape, to write the sentences
      against patterns within the 120-character limit. One shape, two wordings
      (D3, D7).

## 2. The notation

- [x] 2.1 Design the set-value rule-out mark and its input (mouse, keyboard, touch)
      in the reference aid; write `design.md`. D4, D5.
- [x] 2.2 Seed the marks into the recorder's cube; have `findMistakes` vouch for them.
      D2, D6.

## 3. The hint

- [x] 3.1 Place each cited elimination as a step beside the firing that cites it. D7.
- [x] 3.2 Update `docs/games/hints.md` § "Narrate by what survives (Subsets)", which
      describes the eliminations as deliberately not steps.
- [x] 3.3 Update `help/games/subsets.md`.

## 4. Done

- [x] 4.1 Run the app (Chrome, 2026-09-19): a tally press struck two sets out of a
      focused cell; the hint walked to a rule-out step and its collapse; the
      keyboard reached the tally and ruled a set out.
- [x] 4.2 Owner acceptance: a new input, and a changed reference aid (2026-09-19).
