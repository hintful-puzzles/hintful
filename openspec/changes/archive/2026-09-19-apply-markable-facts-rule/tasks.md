# apply-markable-facts-rule — tasks

## 1. Audit

- [x] 1.1 For every game `engine/testing/hint-games.ts` enrolls, read what each step
      kind's sentence rests on, and record conforming or not in `audit.md`, with
      the fact at issue. Key on what the sentence cites, not on a word list.
- [x] 1.2 Confirm Slant's equivalence step is the only nonconforming arm in Slant.

## 2. Follow-ups

- [x] 2.1 Scaffold one notation change per nonconforming game, starting with Slant.
      Four changes: `add-slant-notation` and `add-subsets-notation` need a new
      notation. `note-crossing-deep-eliminations` and `strike-before-forced-singles`
      need none, because the notes exist and the plan never placed them.
- [x] 2.2 Mark `docs/games/hints.md` § "The honest chain tier" and § "Narrate by
      what survives (Subsets)" as breaking rule 6, and add how to check a game to
      § "Give the facts a notation (Loopy)".
