# rome-hint-survives-a-restored-note — tasks

- [x] 1.1 Pin the reproducer from the proposal as a failing test.
      `rome-hint.test.ts`, as the move log on its desc; red with the crash.
- [x] 1.2 Find why the plan skips the strike; decide whether the cause is in
      Rome or in the shared candidate plan. The note was off the grid, not
      restored: Rome's, at input. The restored-note hypothesis was refuted by
      a sweep (proposal).
- [x] 1.3 Fix, prove the pinned test red without the fix, and run the
      cross-game hint walks for every game the fix reaches. Each input path and
      the `executeMove` mask proved red on its own; every cross-game case
      naming Rome or Salad passes.
- [x] 1.4 Answer "Rome only?" from the code: found Salad's leg-only tidy
      strike; fixed as a rung, both routes pinned and proved red.
- [x] 1.5 Guides and spec deltas.
- [x] 1.6 Run the app: Rome's refusal and Salad's tidy step in Chrome.
