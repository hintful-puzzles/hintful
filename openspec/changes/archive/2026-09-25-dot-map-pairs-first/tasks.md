# dot-map-pairs-first — tasks

- [x] 1 `premiseDots` + `journey` shared by the pair and chain rungs.
- [x] 2 `pairDot` / `pairTrim` sentences.
- [x] 3 Tests: `pairDot` pinned, `pairTrim` built by hand, the pair step's
      regions shown with their two dots (all three shown to fail with the legs
      removed); the `pairStrike` board now finds its step after the dots;
      snapshot re-baselined.
- [x] 4 Looked at the pair journey in Chrome.
- [x] 5 `docs/games/hints.md` Map section.
