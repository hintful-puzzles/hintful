# §1.1 settled on per-slot rule-outs kept in an answer row (design D1), so the
# deltas below say what that notation is, what the hint built on it does, and
# how the two limitations `compose-guess-rows-without-dragging` shipped with
# were closed by it.
schema: spec-driven
created: 2026-09-21
