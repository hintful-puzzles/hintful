# dot-map-chains-first — tasks

- [x] 1 `chainDots` legs ahead of the chain step, skipping a region already
      showing exactly its two colors; the whole firing one journey.
- [x] 2 `chainDot` / `chainTrim` sentences; the chain sentence reads off the
      dots.
- [x] 3 Tests: `chainDot` pinned, `chainTrim` built by hand, the claim that every
      numbered region shows its two dots when the chain is spoken (all three
      shown to fail with the legs removed); ledger regex updated; snapshot
      re-baselined.
- [x] 4 Looked at the journey's first leg and the chain step in Chrome.
- [x] 5 `docs/games/hints.md` Map section.
