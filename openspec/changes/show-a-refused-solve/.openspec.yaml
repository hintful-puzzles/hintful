# The requirement this extends is not yet located (task 2.3), so the spec delta
# is owed at implementation. Remove `skip_specs` when it is written.
schema: spec-driven
created: 2026-09-13
skip_specs: true
