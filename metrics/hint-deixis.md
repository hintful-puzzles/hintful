# Hint deixis sweep

Generated by `npx vitest run -c scripts/checks/diff.vitest.config.mts hint-deixis`.
**Advisory.** Read the file's header before acting on a row: most rows are
legitimate ties this lexical filter cannot recognize, and the question to ask
of each is *are the two marks the same kind of thing?*

25667 steps examined, 21440 of them displaying a second mark;
506 distinct sentence shapes flagged across 27 games.

| game | sentence shape |
| --- | --- |
| boats | This segment is a boat's left end, so its boat must continue into the square to its right. |
| boats | Row # still needs one more boat square and has just one free square left, so that square must hold a boat segment. |
| boats | This segment is a boat's right end, so its boat must continue to its left, and the square to its right must be water. |
| boats | Column # still needs one more boat square and has just one free square left, so that square must hold a boat segment. |
| boats | This segment is a boat's left end, so its boat must continue to its right, and the square to its left must be water. |
| boats | Filling this square would make a boat of #, and the largest one still missing is #, so it must be water. |
| boats | If this square were water, it would complete a boat the fleet has no room for, so it must hold a boat segment. |
| boats | If this square held a boat segment, it would complete a boat the fleet has no room for, so it must be water. |
| boats | If this square were water, column # could no longer reach its #, so it must hold a boat segment. |
| boats | Every #-boat is already placed, and water or the board's edge surrounds this square, so it must be water. |
| boats | Filling this run would make a boat of #, but every #-boat is already placed, so the free square must be water. |
| bridges | A bridge here would shut these # islands into a finished group of their own, so this way must be blocked. |
| crossing | This run is # squares long, and only one number in the list is # digits, so it must be #. |
| crossing | Only one #-digit number left matches the digits already in this run, so it must be #. |
| crossing | Every other #-digit number is already on the board, so this run must be #. |
| crossing | Every number that still fits this across run puts # or # in this square, so note them. |
| crossing | Every number that still fits this down run puts #, #, # or # in this square, so note them. |
| crossing | Every number that still fits this across run has a # in this square, so it must be #. |
| crossing | Across, this square can only be # or #, and the down number rules out all but #, so it must be #. |
| crossing | Every number that still fits this down run has an # in this square, so it must be #. |
| crossing | Down, this square can only be #, #, # or #, and the across number rules out all but #, so it must be #. |
| crossing | Every number that still fits this down run has a # in this square, so it must be #. |
| crossing | Across, this square can only be #, # or #, and the down number rules out all but #, so it must be #. |
| crossing | Down, this square can only be # or #, and the across number rules out all but #, so it must be #. |
| crossing | Down, this square can only be #, # or #, and the across number rules out all but #, so it must be #. |
| dominosa | Every remaining spot for the outlined domino overlaps this pair, so no other domino can go here. |
| dominosa | A domino here would leave both outlined squares needing one and the same domino, a duplicate, so it can't. |
| dominosa | The outlined square can only be part of the #–# domino, so #–# can't sit here instead. |
| dominosa | A #–# domino here would force a second #–# at the outlined square, but each domino is used once, so it can't. |
| dominosa | The outlined squares can only hold one set of dominoes, which uses the #–#, so #–# can't sit here as well. |
| filling | The outlined region of # can't fully grow without this square. |
| filling | The outlined region of # has only this one empty square to grow into. |
| galaxies | Only the ringed white dot can own this cell: for any other dot, its partner cell is off the board or on a dot. |
| group | In this row, b can go in only this cell, since every other cell in the row rules it out, so it must be b. |
| group | In this row, d can go in only this cell, since every other cell in the row rules it out, so it must be d. |
| group | In this column, f can go in only this cell, since every other cell in the column rules it out, so it must be f. |
| group | In this row, c can go in only this cell, since every other cell in the row rules it out, so it must be c. |
| group | In this row, a can go in only this cell, since every other cell in the row rules it out, so it must be a. |
| group | In this column, b can go in only this cell, since every other cell in the column rules it out, so it must be b. |
| group | In this column, d can go in only this cell, since every other cell in the column rules it out, so it must be d. |
| group | In this row, e can go in only this cell, since every other cell in the row rules it out, so it must be e. |
| group | In this column, e can go in only this cell, since every other cell in the column rules it out, so it must be e. |
| group | In this row, f can go in only this cell, since every other cell in the row rules it out, so it must be f. |
| group | d·b = d shows b is the identity, so its row and column are just the element labels, and this cell must be a. |
| group | The identity's row and column are just the element labels, so this cell must be a. |
| group | The identity's row and column are just the element labels, so this cell must be b. |
| group | The identity's row and column are just the element labels, so this cell must be c. |
| group | The identity's row and column are just the element labels, so this cell must be d. |
| group | The identity's row and column are just the element labels, so this cell must be e. |
| group | The identity's row and column are just the element labels, so this cell must be f. |
| group | In this column, a can go in only this cell, since every other cell in the column rules it out, so it must be a. |
| group | There's already an a in this row and column, so we must cross out the a from the other cells they pass through. |
| group | There's already an e in this row and column, so we must cross out the e from the other cells they pass through. |
| group | There's already an h in this row and column, so we must cross out the h from the other cells they pass through. |
| group | There's already a d in this row and column, so we must cross out the d from the other cells they pass through. |
| group | There's already a g in this row and column, so we must cross out the g from the other cells they pass through. |
| group | There's already an f in this row and column, so we must cross out the f from the other cells they pass through. |
| group | There's already a c in this row and column, so we must cross out the c from the other cells they pass through. |
| group | There's already a b in this row and column, so we must cross out the b from the other cells they pass through. |
| group | In this column, h can go in only this cell, since every other cell in the column rules it out, so it must be h. |
| group | In this row, g can go in only this cell, since every other cell in the row rules it out, so it must be g. |
| group | In this row, h can go in only this cell, since every other cell in the row rules it out, so it must be h. |
| group | In this column, l can go in only this cell, since every other cell in the column rules it out, so it must be l. |
| group | In this column, k can go in only this cell, since every other cell in the column rules it out, so it must be k. |
| group | In this row, k can go in only this cell, since every other cell in the row rules it out, so it must be k. |
| group | In this row, i can go in only this cell, since every other cell in the row rules it out, so it must be i. |
| group | In this column, j can go in only this cell, since every other cell in the column rules it out, so it must be j. |
| group | In this row, j can go in only this cell, since every other cell in the row rules it out, so it must be j. |
| group | In this row, l can go in only this cell, since every other cell in the row rules it out, so it must be l. |
| group | In this column, i can go in only this cell, since every other cell in the column rules it out, so it must be i. |
| group | In this column, c can go in only this cell, since every other cell in the column rules it out, so it must be c. |
| keen | No way to make this cage multiply to # puts # in this cell, so it must be crossed out. |
| keen | No way to make this cage multiply to # puts # or # in this cell, so they must be crossed out. |
| keen | In this row, # can go in only this cell, since every other cell in the row rules it out, so it must be #. |
| keen | There's already a # in this row and column, so we must cross out the # from the other cells they pass through. |
| keen | No way to make this cage differ by # puts # in this cell, so it must be crossed out. |
| keen | In this column, # can go in only this cell, since every other cell in the column rules it out, so it must be #. |
| keen | No way to make this cage sum to # puts # in this cell, so it must be crossed out. |
| keen | No way to make this cage have a ratio of # puts # in this cell, so it must be crossed out. |
| keen | No way to make this cage sum to # puts # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage multiply to # puts #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage multiply to # puts #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage have a ratio of # puts # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage differ by # puts #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage differ by # puts # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage differ by # puts #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage sum to # puts #, # or # in this cell, so they must be crossed out. |
| keen | This cage must multiply to #, and every way to fill it places a # in this column, so the # here must be crossed out. |
| keen | This cage must sum to #, and every way to fill it places a # in this row, so the # here must be crossed out. |
| keen | This cage must differ by #, and every way to fill it places a # in this row, so the # here must be crossed out. |
| keen | This cage must sum to #, and every way to fill it places a # in this column, so the # here must be crossed out. |
| keen | No way to make this cage have a ratio of # puts #, # or # in this cell, so they must be crossed out. |
| keen | This cage must multiply to #, and every way to fill it places a # in this row, so the # here must be crossed out. |
| keen | No way to make this cage multiply to # puts #, #, #, #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage sum to # puts #, #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage have a ratio of # puts #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage multiply to # puts #, #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage sum to # puts #, #, #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage have a ratio of # puts #, #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage differ by # puts #, #, #, #, # or # in this cell, so they must be crossed out. |
| keen | There's already an # in this row and column, so we must cross out the # from the other cells they pass through. |
| keen | No way to make this cage differ by # puts #, #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage multiply to # puts #, #, #, #, #, #, # or # in this cell, so they must be crossed out. |
| keen | No way to make this cage multiply to # puts #, #, #, #, # or # in this cell, so they must be crossed out. |
| lightup | The ringed square is still dark and only this square can still light it, so this one must hold a bulb. |
| loopy | The ringed dot has no line and no other open edge, so a line here would dead-end: this edge can't be a line. |
| loopy | The line at the ringed dot has no other way to go on, so this edge must be a line. |
| loopy | The ringed dot already has its two lines, so this edge can't be a line. |
| loopy | This # already has all # of its lines, so this edge can't be a line. |
| loopy | This edge would close the marked lines into a loop that leaves other lines out, so it can't be a line. |
| loopy | This # already has its line, so this edge can't be a line. |
| loopy | This edge would close the marked lines into a loop with the outlined clue still unmet, so it can't be a line. |
| loopy | This edge closes the marked lines into one loop that meets every clue, so it must be a line. |
| loopy | This # already has its line, counting the marked corner as one, so this edge can't be a line. |
| loopy | The marked corner across the ringed dot needs a line, so this corner can take one line at most. |
| loopy | This #'s other edges can give it # at most, counting the marked corner as one, so this corner needs a line. |
| loopy | The marked corner takes exactly one line; the ringed dot has no line yet, so this edge must be its second. |
| loopy | The marked corner takes exactly one of the ringed dot's two lines, so this corner needs the other. |
| loopy | This #'s other edges already give it #, counting the marked corner as one, so this corner can take one line at most. |
| loopy | The ringed dot has no line; the marked pair makes two of its open edges give # or #, so this edge can't be a line. |
| loopy | Both ringed dots already have a line, so joining them leaves this # short. That edge is out; the other # are lines. |
| loopy | The ringed dot has no line; the marked pair makes two of its open edges give exactly #, so this edge must be a line. |
| loopy | This #'s other edges can give it only #, counting the marked corner as one, so this edge must be a line. |
| loopy | The marked corner can take one line at most; one edge there is already a line, so this edge can't be one. |
| loopy | The ringed dot has one line; the marked pair makes two of its open edges give exactly #, so this edge can't be a line. |
| loopy | The ringed dot has one line; the marked pair makes two of its open edges give # or #, so this edge must be a line. |
| loopy | This # already has all #, counting the marked corner as one, so this edge can't be a line. |
| loopy | This # needs # more; the marked pair makes two of its open edges give exactly #, so this edge can't be a line. |
| loopy | This # already has all #, counting each marked corner as one, so this edge can't be a line. |
| loopy | This #'s other edges can give it only #, counting each marked corner as one, so this edge must be a line. |
| loopy | This edge would close the marked lines into a loop with the outlined clues still unmet, so it can't be a line. |
| loopy | This #'s other edges already give it #, counting each marked corner as one, so this corner can take one line at most. |
| loopy | This #'s other edges can give it # at most, counting each marked corner as one, so this corner needs a line. |
| loopy | This # needs # more; the marked pair makes two of its open edges give exactly #, so this edge must be a line. |
| mathrax | The #+ clue means this cell and the # across it add to #, so we must cross out #, # and #. |
| mathrax | There's already a # in this row and column, so we must cross out the # from the other cells they pass through. |
| mathrax | The #+ clue means this cell and the # across it add to #, so we must cross out #, #, # and #. |
| mathrax | The #+ clue means this cell and the # across it add to #, so we must cross out #. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out # and #. |
| mathrax | In this column, # can go in only this cell, since every other cell in the column rules it out, so it must be #. |
| mathrax | In this row, # can go in only this cell, since every other cell in the row rules it out, so it must be #. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out #, #, # and #. |
| mathrax | The = clue means this cell and the # across it are equal, so we must cross out # and #. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out #. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out #, # and #. |
| mathrax | The = clue means this cell and the # across it are equal, so we must cross out #. |
| mathrax | The #+ clue means this cell and the # across it add to #, so we must cross out # and #. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out #, #, #, #, #, #, # and #. |
| mathrax | The = clue means this cell and the # across it are equal, so we must cross out #, #, #, #, #, #, # and #. |
| mathrax | The #× clue means this cell and the # across it multiply to #, so we must cross out #, #, #, #, #, # and #. |
| mathrax | The #+ clue means this cell and the # across it add to #, so we must cross out #, #, #, # and #. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out #, #, #, # and #. |
| mathrax | The #÷ clue means this cell and the # across it divide to give #, so we must cross out #, #, #, # and #. |
| mathrax | There's already an # in this row and column, so we must cross out the # from the other cells they pass through. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out #, #, #, #, #, # and #. |
| mathrax | The #+ clue means this cell and the # across it add to #, so we must cross out #, #, #, #, #, # and #. |
| mathrax | The #÷ clue means this cell and the # across it divide to give #, so we must cross out # and #. |
| mathrax | The #+ clue means this cell and the # across it add to #, so we must cross out #, #, #, #, # and #. |
| mathrax | The #× clue means this cell and the # across it multiply to #, so we must cross out #, #, # and #. |
| mathrax | The = clue means this cell and the # across it are equal, so we must cross out #, #, # and #. |
| mathrax | The #÷ clue means this cell and the # across it divide to give #, so we must cross out #, #, #, #, #, #, # and #. |
| mathrax | The #− clue means this cell and the # across it differ by #, so we must cross out #, #, #, #, # and #. |
| netslide | Column # never slides, so only a row move can shift this corner: take it to column #, where it belongs. |
| netslide | Row # never slides, so only a column move can shift this corner: take it to row #, where it belongs. |
| netslide | Column # never slides, so only a row move can shift this corner: take it to column # (setting up). |
| palisade | …and this edge can't be a wall either. |
| palisade | Joining these two regions would make # cells, but a region here holds #, so this edge must be a wall. |
| palisade | This region has # of its # cells and just one way left to grow, so this edge can't be a wall. |
| palisade | Clue # needs all its remaining edges to be walls, so this one must be a wall. |
| palisade | Clue # already has all its walls, so this edge can't be one. |
| palisade | A wall can't stop in mid-air at this corner, so this edge must be a wall. |
| palisade | …and this edge must be a wall too. |
| pattern | This column's run of # can slide only # cells, so these cells must be black. |
| pattern | This column's run of # can slide only # cell, so these cells must be black. |
| pattern | No run can reach these cells in this row, so they must be white. |
| pattern | No run can reach this cell in this column, so it must be white. |
| pattern | No run can reach this cell in this row, so it must be white. |
| pattern | This row's run of # can slide only # cells, so this cell must be black. |
| pattern | This column's run of # has nowhere to slide, so these cells must be black. |
| pattern | This row's run of # has nowhere to slide, so this cell must be black. |
| pattern | This column's run of # has nowhere to slide, so this cell must be black. |
| pattern | This column's run of # can slide only # cell, so this cell must be black. |
| pattern | This column's run of # can slide only # cells, so this cell must be black. |
| pattern | No run can reach these cells in this column, so they must be white. |
| pattern | This row's run of # has nowhere to slide, so these cells must be black. |
| pattern | This row's run of # can slide only # cell, so this cell must be black. |
| pattern | This row's run of # can slide only # cells, so these cells must be black. |
| pattern | This row's run of # can slide only # cell, so these cells must be black. |
| salad | This row's left-hand clue sees C first and this square is nearest to it, so only C can go here: cross out A and B. |
| salad | This column's top clue sees A first and this square is nearest to it, so only A can go here: cross out B. |
| salad | This column's bottom clue sees A first and this square is nearest to it, so only A can go here: cross out B and C. |
| salad | This row's left-hand clue sees A first and this square is nearest to it, so only A can go here: cross out B and C. |
| salad | This column's bottom clue sees C first and this square is nearest to it, so only C can go here: cross out A and B. |
| salad | This row's right-hand clue sees C first and this square is nearest to it, so only C can go here: cross out B. |
| salad | In this row, B can go in only this square, since every other square in the row rules it out, so it must be B. |
| salad | There's already a B in this row and column, so we must cross out the B from the other squares they pass through. |
| salad | In this column, C can go in only this square, since every other square in the column rules it out, so it must be C. |
| salad | There's already a C in this row and column, so we must cross out the C from the other squares they pass through. |
| salad | In this row, A can go in only this square, since every other square in the row rules it out, so it must be A. |
| salad | There's already an A in this row and column, so we must cross out the A from the other squares they pass through. |
| salad | All # letters of this column are already placed, so every other square in it must be empty. |
| salad | This row already has its one empty square, so every other square in it must hold a letter. |
| salad | Every other letter has been ruled out in this square, so it can only be C. |
| salad | Every other letter has been ruled out in this square, so it can only be A. |
| salad | This square is now known to hold a letter, so we must cross out its empty-square mark. |
| salad | This row's left-hand clue sees B first and this square is nearest to it, so only B can go here: cross out C. |
| salad | This column already has its one empty square, so every other square in it must hold a letter. |
| salad | Every other letter has been ruled out in this square, so it can only be B. |
| salad | This column's bottom clue sees C first and this square is nearest to it, so only C can go here: cross out B. |
| salad | This column's top clue sees C first and this square is nearest to it, so only C can go here: cross out B. |
| salad | We already know which # squares of this column hold its letters, so every other square in it must be empty. |
| salad | We already know which # squares of this row hold its letters, so every other square in it must be empty. |
| salad | This row already has both of its empty squares, so every other square in it must hold a number. |
| salad | We already know which # squares of this column hold its numbers, so every other square in it must be empty. |
| salad | In this row, # can go in only this square, since every other square in the row rules it out, so it must be #. |
| salad | There's already a # in this row and column, so we must cross out the # from the other squares they pass through. |
| salad | This square is now known to hold a number, so we must cross out its empty-square mark. |
| salad | Every other number has been ruled out in this square, so it can only be #. |
| salad | All # numbers of this column are already placed, so every other square in it must be empty. |
| salad | All # numbers of this row are already placed, so every other square in it must be empty. |
| salad | This column already has both of its empty squares, so every other square in it must hold a number. |
| salad | We already know which # squares of this row hold its numbers, so every other square in it must be empty. |
| salad | This row's left-hand clue sees C first and this square is nearest to it, so only C can go here: cross out A, B and D. |
| salad | This column's bottom clue sees C first and this square is nearest to it, so only C can go here: cross out A, B and D. |
| salad | This column's top clue sees D first and this square is nearest to it, so only D can go here: cross out C. |
| salad | This row's left-hand clue sees B first and this square is nearest to it, so only B can go here: cross out A and D. |
| salad | This row's right-hand clue sees A first and this square is nearest to it, so only A can go here: cross out B and E. |
| salad | This row's left-hand clue sees E first and this square is nearest to it, so only E can go here: cross out A, B and D. |
| salad | This column already has all # of its empty squares, so every other square in it must hold a letter. |
| salad | This column's bottom clue sees B first and this square is nearest to it, so only B can go here: cross out A, D and E. |
| salad | This column's bottom clue sees A first and this square is nearest to it, so only A can go here: cross out B, D and E. |
| salad | This row's right-hand clue sees D first and this square is nearest to it, so only D can go here: cross out A, B and C. |
| salad | In this column, D can go in only this square, since every other square in the column rules it out, so it must be D. |
| salad | There's already a D in this row and column, so we must cross out the D from the other squares they pass through. |
| salad | This column's top clue sees D first and this square is nearest to it, so only D can go here: cross out B and E. |
| salad | This column's bottom clue sees A first and this square is nearest to it, so only A can go here: cross out B and E. |
| salad | This column's top clue sees E first and this square is nearest to it, so only E can go here: cross out A. |
| salad | This row already has all # of its empty squares, so every other square in it must hold a letter. |
| salad | Every other letter has been ruled out in this square, so it can only be E. |
| salad | There's already an E in this row and column, so we must cross out the E from the other squares they pass through. |
| salad | In this column, E can go in only this square, since every other square in the column rules it out, so it must be E. |
| salad | In this column, A can go in only this square, since every other square in the column rules it out, so it must be A. |
| salad | Every other letter has been ruled out in this square, so it can only be D. |
| salad | In this row, E can go in only this square, since every other square in the row rules it out, so it must be E. |
| salad | In this column, B can go in only this square, since every other square in the column rules it out, so it must be B. |
| salad | All # letters of this row are already placed, so every other square in it must be empty. |
| salad | This row's right-hand clue sees D first and this square is nearest to it, so only D can go here: cross out B, C and E. |
| salad | This column's top clue sees B first and this square is nearest to it, so only B can go here: cross out C and E. |
| salad | This column's top clue sees C first and this square is nearest to it, so only C can go here: cross out B, D and E. |
| salad | This column's bottom clue sees D first and this square is nearest to it, so only D can go here: cross out A, B and E. |
| salad | This row's left-hand clue sees A first and this square is nearest to it, so only A can go here: cross out B and E. |
| salad | This column's bottom clue sees D first and this square is nearest to it, so only D can go here: cross out A, B, C and E. |
| salad | This row's right-hand clue sees C first and this square is nearest to it, so only C can go here: cross out A, B, D and E. |
| salad | This row's left-hand clue sees E first and this square is nearest to it, so only E can go here: cross out A, B and C. |
| salad | This column's bottom clue sees C first and this square is nearest to it, so only C can go here: cross out A, B, D and E. |
| salad | This column's top clue sees A first and this square is nearest to it, so only A can go here: cross out B, C and E. |
| salad | This row's left-hand clue sees C first and this square is nearest to it, so only C can go here: cross out A, B, D and E. |
| salad | This row's right-hand clue sees B first and this square is nearest to it, so only B can go here: cross out C, D and E. |
| salad | This column's bottom clue sees D first and this square is nearest to it, so only D can go here: cross out B, C and E. |
| salad | This row's left-hand clue sees A first and this square is nearest to it, so only A can go here: cross out B, D and E. |
| salad | This column's bottom clue sees C first and this square is nearest to it, so only C can go here: cross out A, B and E. |
| salad | In this row, C can go in only this square, since every other square in the row rules it out, so it must be C. |
| seismic | No other cell in the outlined area can still be #, so this cell must be #. |
| seismic | The outlined area can put its # only in line with this cell and within # cells of it, so this cell can't be #. |
| singles | This # shares a line with the ringed white #, which already uses that number, so this copy must be shaded. |
| singles | These #s share a line with the ringed white #, which already uses that number, so they must be shaded. |
| slant | The same clue forces this square too, so it must slant toward the clue. |
| slant | The same clue forces this square too, so it must slant away. |
| slant | Two corners of this square are already joined by a chain of diagonals, so it must slant the other way to avoid a loop. |
| slant | This square is marked to slant the same as the ringed one, so it must be a forward slash too. |
| slant | These points have one way out each; linking them here would seal a loop, so this square must slant the other way. |
| slant | This square is marked to slant the same as the ringed one, so it must be a backslash too. |
| solo | A # is placed here, so it can't repeat in its row, column or block: cross out the # from these cells. |
| solo | In this row, # can go in only this cell, since every other cell in the row rules it out, so it must be #. |
| solo | In this block, # can go in only this cell, since every other cell in the block rules it out, so it must be #. |
| solo | In this column, # can go in only this cell, since every other cell in the column rules it out, so it must be #. |
| solo | In this block, every cell that can still take # lies in this row, so # must be crossed out of the rest of it. |
| solo | Other cells in this block already account for #, so it must be crossed out here. |
| solo | Other cells in this block already account for # and #, so they must be crossed out here. |
| solo | In this row, every cell that can still take # lies in this block, so # must be crossed out of the rest of it. |
| solo | Other cells in this column already account for #, so it must be crossed out here. |
| solo | A # is placed here, so it can't repeat in its row, column, block or diagonal: cross out the # from these cells. |
| solo | An # is placed here, so it can't repeat in its row, column or block: cross out the # from these cells. |
| solo | In this diagonal, # can go in only this cell, since every other cell in the diagonal rules it out, so it must be #. |
| solo | An # is placed here, so it can't repeat in its row, column, block or diagonal: cross out the # from these cells. |
| solo | In this block, every cell that can still take # lies in this column, so # must be crossed out of the rest of it. |
| solo | Cell # is # or #, and every numbered cell has just two numbers left, so each forces the next. If cell # is #, this cell's column already has it; if #, cell # is driven to #, in line with this cell. Either way, cross out # here. |
| solo | Other cells in this column already account for # and #, so they must be crossed out here. |
| solo | This block must total #; the cages and digits inside it account for all but #, so its one open cell must be #. |
| solo | A # is placed here, so it can't repeat in its row, column, block or cage: cross out the # from these cells. |
| solo | An # is placed here, so it can't repeat in its row, column, block or cage: cross out the # from these cells. |
| solo | This row must total #; the cages and digits inside it account for all but #, so its one open cell must be #. |
| solo | This column must total #; the cages and digits inside it account for all but #, so its one open cell must be #. |
| solo | In this row, a can go in only this cell, since every other cell in the row rules it out, so it must be a. |
| solo | An a is placed here, so it can't repeat in its row, column or block: cross out the a from these cells. |
| solo | A c is placed here, so it can't repeat in its row, column or block: cross out the c from these cells. |
| solo | A b is placed here, so it can't repeat in its row, column or block: cross out the b from these cells. |
| solo | In this row, b can go in only this cell, since every other cell in the row rules it out, so it must be b. |
| solo | In this column, a can go in only this cell, since every other cell in the column rules it out, so it must be a. |
| solo | In this row, c can go in only this cell, since every other cell in the row rules it out, so it must be c. |
| solo | In this column, b can go in only this cell, since every other cell in the column rules it out, so it must be b. |
| solo | In this column, c can go in only this cell, since every other cell in the column rules it out, so it must be c. |
| spokes | Just enough free spokes left for this hub's count, so they must all be lines. |
| spokes | And this one must be a line too. |
| spokes | Connecting two #-hubs would strand them from the rest, so rule out this spoke. |
| spokes | Only one free spoke left for this hub's count, so it must be a line. |
| spokes | This hub already has all its lines, so none of its other spokes can be one. Rule them out. |
| spokes | And rule this one out too. |
| spokes | Drawing this line would force two diagonals to cross, so rule it out. |
| spokes | Drawing this line would over-fill the ringed hub, so rule it out. |
| spokes | Drawing this line would strand the ringed hubs, so rule it out. |
| sticks | The black # takes no lines, and a horizontal line here would run straight into it. So this # must be vertical. |
| sticks | The black # rules this square out too: a vertical line here would run into it. So this square must be horizontal. |
| sticks | The # needs a longer line, and a horizontal line here would leave it only # square. So this # must be vertical. |
| sticks | A vertical line here would run the #'s line to # squares, too long for it. So this square must be horizontal. |
| sticks | The # needs a longer line, and a horizontal line here would leave it only # square. So this square must be vertical. |
| sticks | The # rules this square out too: a horizontal line would leave it only # squares. So this square must be vertical. |
| sticks | The # rules this square out too: a vertical line would leave it only # squares. So this # must be horizontal. |
| sticks | A vertical line here would join the # and the # into one line. So this # must be horizontal. |
| sticks | A horizontal line here would run the #'s line to # squares, too long for it. So this square must be vertical. |
| sticks | The black # needs both of its open sides, and a vertical line here would close one. So this # must be horizontal. |
| sticks | The # needs a longer line, and a vertical line here would leave it only # square. So this square must be horizontal. |
| sticks | The # rules this square out too: a horizontal line would run its line to # squares. So this square must be vertical. |
| sticks | A vertical line here would run the #'s line to # squares, too long for it. So this # must be horizontal. |
| sticks | The black # has one open side left, and a horizontal line here would close it off. So this square must be vertical. |
| sticks | The # needs a longer line, and a horizontal line here would leave it only # squares. So this square must be vertical. |
| sticks | The # needs a longer line, and a vertical line here would leave it only # squares. So this # must be horizontal. |
| sticks | The # needs a longer line, and a vertical line here would leave it only # squares. So this square must be horizontal. |
| sticks | The # rules this square out too: a vertical line would leave it only # squares. So this square must be horizontal. |
| sticks | The # needs a longer line, and a vertical line here would leave it only # square. So this # must be horizontal. |
| sticks | The # rules this square out too: a vertical line would leave it only # square. So this square must be horizontal. |
| sticks | The black # already has its # lines, and a horizontal line here would add another. So this square must be vertical. |
| sticks | The black # has one open side left, and a vertical line here would close it off. So this # must be horizontal. |
| sticks | The # needs a longer line, and a horizontal line here would leave it only # squares. So this # must be vertical. |
| sticks | The black # already has its # lines, and a horizontal line here would add another. So this # must be vertical. |
| sticks | The black # already has its # line, and a horizontal line here would add another. So this square must be vertical. |
| sticks | The black # needs both of its open sides, and a horizontal line here would close one. So this # must be vertical. |
| sticks | The black # rules this square out too: a horizontal line would close another open side. So this square must be vertical. |
| sticks | The black # needs all # of its open sides, and a horizontal line here would close one. So this square must be vertical. |
| sticks | The black # rules this square out too: a vertical line would close another open side. So this # must be horizontal. |
| sticks | The black # already has its # line, and a horizontal line here would add another. So this # must be vertical. |
| sticks | The black # rules this square out too: a horizontal line here would run into it. So this square must be vertical. |
| sticks | A horizontal line here would run the #'s line to # squares, too long for it. So this # must be vertical. |
| sticks | The black # takes no lines, and a vertical line here would run straight into it. So this square must be horizontal. |
| sticks | The black # rules this square out too: a horizontal line here would run into it. So this # must be vertical. |
| sticks | The # rules this square out too: a vertical line would run its line to # squares. So this square must be horizontal. |
| sticks | The black # takes no lines, and a horizontal line here would run straight into it. So this square must be vertical. |
| sticks | The # rules this square out too: a horizontal line would leave it only # square. So this square must be vertical. |
| sticks | The black # needs both of its open sides, and a horizontal line here would close one. So this square must be vertical. |
| sticks | The black # rules this square out too: a horizontal line would close another open side. So this # must be vertical. |
| sticks | A vertical line here would join two #s into one line. So this square must be horizontal. |
| sticks | The black # needs both of its open sides, and a vertical line here would close one. So this square must be horizontal. |
| sticks | The black # rules this square out too: a vertical line here would run into it. So this # must be horizontal. |
| sticks | A horizontal line here would join the # and the # into one line. So this # must be vertical. |
| sticks | The black # has one open side left, and a vertical line here would close it off. So this square must be horizontal. |
| sticks | The # rules this square out too: a horizontal line would leave it only # squares. So this # must be vertical. |
| sticks | The black # already has its # line, and a vertical line here would add another. So this square must be horizontal. |
| sticks | The black # needs all # of its open sides, and a vertical line here would close one. So this square must be horizontal. |
| sticks | The black # rules this square out too: a vertical line would close another open side. So this square must be horizontal. |
| subsets | The highlighted cell's set lies inside this one and has B marked, so B must be here too. Mark B present. |
| subsets | The highlighted cell's set lies inside this one and has A marked, so A must be here too. Mark A present. |
| subsets | Still filling this cell: the highlighted cell's B is marked too, so mark B present here. |
| subsets | This cell's set lies inside the highlighted cell's, which has no D, so D can't be here either. Clear D. |
| subsets | The highlighted set can go nowhere but this cell. It has no A, so clear A. |
| subsets | Still filling this cell: the highlighted set has no B either, so clear B here. |
| subsets | Still filling this cell: the highlighted set has no C either, so clear C here. |
| subsets | Still filling this cell: the highlighted set has no D either, so clear D here. |
| subsets | The highlighted set can go nowhere but this cell. It contains C, so mark C present. |
| subsets | Still filling this cell: the highlighted set also contains D, so mark D present here. |
| subsets | Only the highlighted sets can still go in this cell. They all contain C, so mark C present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | The highlighted cell's set lies inside this one and has C marked, so C must be here too. Mark C present. |
| subsets | Only the highlighted set can still go in this cell. It contains C, so mark C present. For instance, {B} is already placed on the board (highlighted). |
| subsets | Still filling this cell: the highlighted set has no A either, so clear A here. |
| subsets | Only the highlighted sets can still go in this cell. None of them has B, so clear B. For instance, {A,B,C} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. None of them has B, so clear B. For instance, {B,C} is already placed on the board (highlighted). |
| subsets | This cell's set lies inside the highlighted cell's, which has no B, so B can't be here either. Clear B. |
| subsets | Still filling this cell: the highlighted set also contains B, so mark B present here. |
| subsets | Still filling this cell: the highlighted set also contains C, so mark C present here. |
| subsets | The highlighted cell's set lies inside this one and has D marked, so D must be here too. Mark D present. |
| subsets | The highlighted set can go nowhere but this cell. It contains A, so mark A present. |
| subsets | The highlighted set can go nowhere but this cell. It has no C, so clear C. |
| subsets | This cell's set lies inside the highlighted cell's, which has no A, so A can't be here either. Clear A. |
| subsets | Still filling this cell: the highlighted cell has no C either, so clear C here. |
| subsets | The highlighted set can go nowhere but this cell. It has no B, so clear B. |
| subsets | No highlighted set is a smaller set inside {B}, so the horseshoe to the highlighted cell rules {B} out here. |
| subsets | No highlighted set is a smaller set inside {B,C}, so the horseshoe to the highlighted cell rules {B,C} out here. |
| subsets | No highlighted set is a smaller set inside {D}, so the horseshoe to the highlighted cell rules {D} out here. |
| subsets | No highlighted set is a smaller set inside {C,D}, so the horseshoe to the highlighted cell rules {C,D} out here. |
| subsets | Only the highlighted sets can still go in this cell. They all contain A, so mark A present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | No highlighted set is a smaller set inside {A}, so the horseshoe to the highlighted cell rules {A} out here. |
| subsets | No highlighted set is a smaller set inside {A,D}, so the horseshoe to the highlighted cell rules {A,D} out here. |
| subsets | Only the highlighted set can still go in this cell. It contains A, so mark A present. For instance, {A,B} is already placed on the board (highlighted). |
| subsets | This cell's set lies inside the highlighted cell's, which has no C, so C can't be here either. Clear C. |
| subsets | Only the highlighted set can still go in this cell. It contains A, so mark A present. For instance, {A,B,D} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain C, so mark C present. For instance, {A,B} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain C, so mark C present. For instance, {C} is already placed on the board (highlighted). |
| subsets | No highlighted set is a smaller set inside {A,C}, so the horseshoe to the highlighted cell rules {A,C} out here. |
| subsets | No highlighted set is a smaller set inside {A,C,D}, so the horseshoe to the highlighted cell rules {A,C,D} out here. |
| subsets | Only the highlighted sets can still go in this cell. They all contain B, so mark B present. For instance, {A,B} is already placed on the board (highlighted). |
| subsets | Still filling this cell: the highlighted sets all contain C too, so mark C present here. |
| subsets | Still filling this cell: none of the highlighted sets has D, so clear D here. |
| subsets | Only the highlighted set can still go in this cell. It contains B, so mark B present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains D, so mark D present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains A, so mark A present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain C, so mark C present. For instance, {A} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain C, so mark C present. For instance, {B} is already placed on the board (highlighted). |
| subsets | Still filling this cell: none of the highlighted sets has A, so clear A here. |
| subsets | Only the highlighted sets can still go in this cell. None of them has B, so clear B. For instance, {C} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains D, so mark D present. For instance, {C} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains B, so mark B present. For instance, {A,B,C,D} is already placed on the board (highlighted). |
| subsets | Still filling this cell: the highlighted cell has no D either, so clear D here. |
| subsets | Still filling this cell: the highlighted cell's D is marked too, so mark D present here. |
| subsets | Only the highlighted sets can still go in this cell. They all contain A, so mark A present. For instance, {B} is already placed on the board (highlighted). |
| subsets | Still filling this cell: none of the highlighted sets has B, so clear B here. |
| subsets | No highlighted set is a smaller set inside {C}, so the horseshoe to the highlighted cell rules {C} out here. |
| subsets | Only the highlighted sets can still go in this cell. They all contain A, so mark A present. For instance, {B,C} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It contains C, so mark C present. |
| subsets | Only the highlighted sets can still go in this cell. They all contain A, so mark A present. For instance, {A} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain D, so mark D present. For instance, {C} is already placed on the board (highlighted). |
| subsets | Still filling this cell: the highlighted cell has no B either, so clear B here. |
| subsets | Only the highlighted set can still go in this cell. It has no A, so clear A. For instance, {A} is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain D, so mark D present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain A, so mark A present. For instance, {C} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It has no C, so clear C. For instance, {C,D} is already placed on the board (highlighted). |
| subsets | No highlighted set is a bigger set holding {A,C,D}, so the horseshoe to the highlighted cell rules {A,C,D} out here. |
| subsets | Only the highlighted set can still go in this cell. It has no D, so clear D. |
| subsets | The highlighted set can go nowhere but this cell. It contains B, so mark B present. |
| subsets | The highlighted set can go nowhere but this cell. It contains D, so mark D present. |
| subsets | Only the highlighted sets can still go in this cell. They all contain B, so mark B present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | Still filling this cell: none of the highlighted sets has C, so clear C here. |
| subsets | Only the highlighted set can still go in this cell. It contains D, so mark D present. For instance, {B,C,D} is already placed on the board (highlighted). |
| subsets | Only the highlighted set can still go in this cell. It has no D, so clear D. For instance, {B,D} is already placed on the board (highlighted). |
| subsets | No highlighted set is a smaller set inside {A,B}, so the horseshoe to the highlighted cell rules {A,B} out here. |
| subsets | No highlighted set is a smaller set inside {A,B,C}, so the horseshoe to the highlighted cell rules {A,B,C} out here. |
| subsets | No highlighted set is a smaller set inside {A,B,D}, so the horseshoe to the highlighted cell rules {A,B,D} out here. |
| subsets | Only the highlighted set can still go in this cell. It contains C, so mark C present. For instance, the empty set is already placed on the board (highlighted). |
| subsets | Only the highlighted sets can still go in this cell. They all contain A, so mark A present. For instance, {B,D} is already placed on the board (highlighted). |
| subsets | No highlighted set is a bigger set holding {A,B,C}, so the horseshoe to the highlighted cell rules {A,B,C} out here. |
| subsets | No highlighted set is a bigger set holding {C,D}, so the horseshoe to the highlighted cell rules {C,D} out here. |
| subsets | Only the highlighted sets can still go in this cell. None of them has C, so clear C. For instance, the empty set is already placed on the board (highlighted). |
| towers | These facing clues sum to one more than the grid size, pinning the tallest tower: height # can only sit here. |
| towers | In this column, height # can go in only this cell, since every other cell in the column rules it out, so it must be #. |
| towers | Clue # sees every tower in this line, so heights must climb #, #, … from the clue: height # can only sit here. |
| towers | Continuing up the line, height # can only sit here. |
| towers | No way for clue # to show exactly # towers puts height # here, so we must cross out the #. |
| towers | In this row, height # can go in only this cell, since every other cell in the row rules it out, so it must be #. |
| towers | Another group of cells already accounts for a fixed set of heights that includes #, so we must cross out the # here. |
| towers | Cell # is # or #, and every numbered cell has just two heights left, so each forces the next. If cell # is #, this cell's column already has it; if #, cell # is driven to #, in line with this cell. Either way, cross out # here. |
| tracks | Only one side of this square is still open, and track needs two, so it must be empty. |
| tracks | This square carries a track with only two of its sides still open, so there's only one way for it to go. |
| tracks | This column already has all # of the track squares its clue allows, so every other square in it must be empty. |
| tracks | This column's clue is # and it is # squares long, so every square in it must carry track. |
| tracks | This column already has both of the track squares its clue allows, so every other square in it must be empty. |
| tracks | This row can leave only # squares empty and they are already marked, so every other square in it must carry track. |
| tracks | This row already has all # of the track squares its clue allows, so every other square in it must be empty. |
| tracks | This column already has the one track square its clue allows, so every other square in it must be empty. |
| tracks | This row can leave only # square empty and it is already marked, so every other square in it must carry track. |
| tracks | This column can leave only # squares empty and they are already marked, so every other square in it must carry track. |
| tracks | With two track squares left in this column and no way across it, this loose end must run straight on. |
| tracks | This row's clue is # and it is # squares long, so every square in it must carry track. |
| tracks | This row already has the one track square its clue allows, so every other square in it must be empty. |
| tracks | Every side of this square is blocked, so no track can reach it: it must be empty. |
| tracks | This column can leave only # square empty and it is already marked, so every other square in it must carry track. |
| tracks | Track here would carry on upward, but this column has room for one more track square, so this must be empty. |
| tracks | The outlined track already joins these two squares, so linking them here would close a loop; this side must be blocked. |
| tracks | Track here would carry on to the right, but this row has room for one more track square, so this must be empty. |
| tracks | Track here would carry on to the left, but this row has room for one more track square, so this must be empty. |
| tracks | Track here would carry on downward, but this column has room for one more track square, so this must be empty. |
| tracks | This row already has both of the track squares its clue allows, so every other square in it must be empty. |
| undead | No ghosts are left to place, so no undecided cell can be one; we must cross out the ghost here. |
| undead | This sightline's # and # leave no room for a vampire here, so we must cross out the vampire. |
| undead | The same sightline rules the vampire out of this cell too. |
| undead | This sightline's # and # leave no room for a zombie here, so we must cross out the zombie. |
| undead | The same sightline rules the zombie out of this cell too. |
| undead | No vampires are left to place, so no undecided cell can be one; we must cross out the vampire here. |
| undead | This sightline's # and # leave no room for a ghost or vampire here, so we must cross out both. |
| undead | No zombies are left to place, so no undecided cell can be one; we must cross out the zombie here. |
| undead | This sightline's # and # leave no room for a ghost here, so we must cross out the ghost. |
| undead | The same sightline rules the ghost out of this cell too. |
| undead | This sightline's # and # leave no room for a ghost or zombie here, so we must cross out both. |
| undead | The same sightline rules the vampire and zombie out of this cell too. |
| undead | This sightline's # and # leave no room for a vampire or zombie here, so we must cross out both. |
| undead | The same sightline rules the ghost and zombie out of this cell too. |
| undead | The same sightline rules the ghost and vampire out of this cell too. |
| unequal | The cell across this greater-than sign is at most #, so this one must be smaller; we must cross out # and #. |
| unequal | The cell across this greater-than sign is at least #, so this one must be larger; we must cross out #. |
| unequal | The cell across this greater-than sign is at least #, so this one must be larger; we must cross out # and #. |
| unequal | The cell across this greater-than sign is at most #, so this one must be smaller; we must cross out #. |
| unequal | The cell across this greater-than sign is at least #, so this one must be larger; we must cross out #, # and #. |
| unequal | There's already a # in this row and column, so we must cross out the # from the other cells they pass through. |
| unequal | In this column, # can go in only this cell, since every other cell in the column rules it out, so it must be #. |
| unequal | In this row, # can go in only this cell, since every other cell in the row rules it out, so it must be #. |
| unequal | The cell across this greater-than sign is at most #, so this one must be smaller; we must cross out #, # and #. |
| unequal | Cell # is # or #, and every numbered cell has just two numbers left, so each forces the next. If cell # is #, this cell's row already has it; if #, cell # is driven to #, in line with this cell. Either way, cross out # here. |
| unequal | The cell across this greater-than sign is at least #, so this one must be larger; we must cross out #, #, # and #. |
| unequal | The cell across this greater-than sign is at most #, so this one must be smaller; we must cross out #, #, # and #. |
| unruly | Two of these three cells are already black, so a third would make three in a row: this cell must be white. |
| unruly | Two of these three cells are already white, so a third would make three in a row: this cell must be black. |
| unruly | This row already holds all of its black cells, so every remaining cell in it must be white. |
| unruly | This column already holds all of its black cells, so every remaining cell in it must be white. |
| unruly | This column already holds all of its white cells, so every remaining cell in it must be black. |
| unruly | This row already holds all of its white cells, so every remaining cell in it must be black. |
| unruly | The last black in this row can only go in a ringed cell without forcing three whites, so the rest must be white. |
| unruly | The last black in this column can only go in a ringed cell without forcing three whites, so the rest must be white. |
| unruly | The last white in this column can only go in a ringed cell without forcing three blacks, so the rest must be black. |
| unruly | The last white in this row can only go in a ringed cell without forcing three blacks, so the rest must be black. |

## Flagged shapes per game

- subsets: 81
- salad: 74
- sticks: 48
- group: 38
- keen: 33
- solo: 30
- loopy: 29
- mathrax: 28
- tracks: 21
- pattern: 16
- undead: 15
- crossing: 13
- unequal: 12
- boats: 11
- unruly: 10
- spokes: 9
- towers: 8
- palisade: 7
- slant: 6
- dominosa: 5
- netslide: 3
- filling: 2
- seismic: 2
- singles: 2
- bridges: 1
- galaxies: 1
- lightup: 1
